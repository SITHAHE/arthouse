# REPORT — гейты фабрики
Дата: 2026-08-01
Проект: E:\AI\Claude\arthouse · Стиль: **stage-violet** (новая карточка) · Архетип: запись

## Гейт 0 — factory-lint (детерминированный)
**10/10 — ни одного маркера.**
Замер на прод-сборке (`npm run build` → `vite preview --port 5207` → `factory-lint --url .../arthouse/`), как требует сам линтер: на dev-сервере вес страницы завышен несжатыми модулями Vite и давал ложный жёлтый.
Вес честный: JS 391 КБ (gzip 122 КБ) + CSS 32 КБ (gzip 7,6 КБ) + 35 фото WebP на 3,6 МБ, из них выше сгиба грузятся 3 кадра, остальные `loading="lazy"`.

Что правилось по замечаниям линтера в ходе гейта:
- H1: кегль и line-height перенесены с вложенного `span` на сам тег `h1` (было «H1 = 16px в 7 строк»).
- Секции с сетками одинаковых карточек: было 3 → стало 0. Строка цифр переверстана несимметрично (первая цифра втрое крупнее, остальные тихой колонкой), фото-лента получила подписи кадров и разную высоту.
- Тап-таргеты: навигация, контакты и ссылки в подвале доведены до 44px по высоте.

## Гейт 0.5 — copy-lint (тексты)
**10/10 — клише не найдены** (210 строк).
Отдельно вычищены em-тире в коротких строках и заголовках: заказчик на них придирается. Осталось только внутри дословных цитат родителей, где менять чужую речь нельзя.

## Гейт 1 — design-ru (визуальный скоринг по скриншотам)

Проход 1 дал **8/10**: найден оранжевый маркер §2 — **мета-лейблы «01 / НАПРАВЛЕНИЯ»** на каждой секции. Машинный линтер их пропустил (его regex смотрит исходный текст, а капс делает CSS), глаз поймал. Исправлено на тихую нумерацию по typography-moves №11: цифра моно-шрифтом, отдельно, без косой черты и капса.

Проход 2 (новые скриншоты):
- Красных маркеров: **0**. Нет градиентного текста, glass-навбара, glow на CTA, канонического ИИ-hero, карточек с иконками, эмодзи, плейсхолдеров, курсора, AI-имиджей — все 35 фото настоящие, из альбомов сообщества.
- Оранжевых: **0**. Ни одной секции карточками; заголовки смещены со 2-й колонки; радиус 0 везде; H1 в одну строку; display-шрифт Unbounded с кириллицей (не Inter/Montserrat); padding секций чередуется sm/base/lg.
- Жёлтых: **0**. Фон off-black с температурой (#140620), один акцент, теней нет вообще, «scroll to explore» и бейджей на hero нет.
- **СЧЁТ: 10/10**

**ХАРАКТЕР (§5): 6/6**
1. Запоминающийся ход — имя ARTHOUSE во всю ширину экрана поверх живого шёлкового света.
2. Крупное медиа как герой — зиг-заг возрастов (фото 7 из 12 колонок), портрет тренера 4:5, полноширинная фото-лента.
3. Смена ритма — лента выходит за контейнер до кромки экрана; ряды возрастов меняют сторону; цифры несимметричны.
4. Типографический контраст — Unbounded 800 капсом ~200px против 16px тела, это 12×.
5. Цветовая позиция — тёмная слива с маджентой; в `history.md` такой палитры нет ни у одного сайта.
6. Деталь ручной работы — svg-искра как разделитель в бегущей строке, хром-плашка на фото тренера, точечная распорка в прайсе, переключение залов на карте, подписи под кадрами ленты.

**Приёмы из брифа видны на скриншотах:** типографика №1 (гигант-hero), №2 (микро-метка перед H1), №10 (гигантские цифры), №17 (бегущая строка площадок); композиция №4 (сплит 7/5 по нижней кромке у тренера), №8 (full-bleed лента), №13 (горизонтальная лента), №7 (ритм плотности); вау — №17 шейдер (тяжёлый, один), №8 marquee, №18 лайтбокс.

## Отдельно для арт-директора: осознанное отступление от общего запрета

`README` фабрики и `trends-2026` §1 запрещают «AI-purple / фиолетовый градиент» как маркер ИИ №1. Здесь фиолетовый — **фирменный цвет клиента**, а не дефолт:

- Палитра шейдера снята пипеткой с обложки сообщества ВК (`_work/arthouse/sample.mjs`, пробы по чистым кускам фона). Замер: обложка — яркость 0.29-0.32, насыщенность 0.67, средний rgb(67,38,78); шейдер — яркость 0.289, насыщенность 0.76, средний rgb(59,27,74).
- Сам шейдер — не mesh-градиент, а flow-поле (пресет «Silk», WebGL1 без библиотек), выбранное заказчиком.
- Фиолетовый живёт **только** в фоне. На кнопках, плашках, тексте и иконках градиента нет ни одного: кнопка — плоская маджента с тёмным текстом (контраст 5.6:1), вторичная — хайрлайн.
- Карантин `index.css` не тронут: `text-gradient`, `glass`, `grid-dots`, `floaty` не используются. Glow нет.
- Зафиксировано в поле `ornament:` карточки `stage-violet`.

## Гейт 2 — art-director (чистый контекст)

**СЧЁТ 10/10 · ХАРАКТЕР 6/6 · цена на вид ~120 000 ₽.** По фиолетовому вердикт прямой: «это НЕ ИИ-неон» — проверены отсутствие glow, обводок, mesh-блобов и градиентов на UI. Вердикт «НА ДОРАБОТКУ» стоял не по чек-листу, а по четырём пунктам вне него. Что сделано:

1. **Прайс из шести «по запросу»** — секция снята целиком по решению заказчика (цифр у студии в публичном доступе нет, ставить выдуманные для реального клиента нельзя). На её месте — **«Что входит в занятия»**: расписание, постановка номера, костюм, отчётный концерт, съёмка, конкурсы и сборы. Вопрос «за что я плачу» закрыт содержанием, сумма называется в ответ на заявку. Оба проверяющих считали пустой прайс главной слабостью — теперь пустой колонки нет.
2. **Нижняя половина без фотографий** — между «Что входит» и «Три шага» встал полноширинный кадр с выезда в Петрозаводск с подписью про конкурсы и сборы.
3. **H1 упирался в правую кромку** — кегль на больших экранах снижен с 14.2vw до 13.6vw, воздух справа появился. Обрезки не было (натуральная ширина слова замерена на пяти ширинах), но композиционно замечание верное.
4. **Кадр светлее собственной карточки стиля** — светлые стопы палитры шейдера опущены на 15%. Винъетку не включал: заказчик задал пресет со значением 0, менять его параметры без спроса не стал.

Мелкие замечания тоже приняты: контраст пунктов меню поднят до fg/85, обрезанный svg-завиток в hero снят, стрелки листания везде переведены на svg тем же штрихом 1.8.

## Гейт 0.5+ — copy-chief (чистый контекст)

**Первый проход 7/10, вердикт «на доработку».** Принято 16 замечаний из 18:

- **Обещание в hero было неисполнимым**: «через полгода на Газпром Арену», при том что через полгода по собственному тексту — отчётный концерт. Переписано: «Через полгода ребёнок выходит на сцену: свой костюм, свет, полный зал. Коллективы студии танцевали в Кремле и на Газпром Арене».
- **Цифра не сходилась со списком рядом**: «9 больших сцен» против 10 позиций в бегущей строке, где «Телевидение» сценой не является. «Телевидение» убрано из строки, подпись стала «сцен и городов, где выступали коллективы».
- **Подпись отзыва противоречила фактам**: «в студии с 2018 года» при студии с 2020-го. В отзыве речь о тренере — подпись исправлена на «с Алёной Геннадьевной с 2018 года».
- Три «три» на одну страницу (идеальные тройки — ИИ-маркер): заголовок возрастов стал «С 4 лет и до взрослых групп», лид переписан прозой.
- «Один тренерский подход», «базовые вайбы», дубль «платить заранее не нужно», четыре разных формулировки одной кнопки, тире в title/og/JSON-LD и в подписях — исправлено. CTA везде «Записаться на пробное».
- Пруф тренера усилен цифрой: «16 лет в хореографии».
- Добавлен вопрос в FAQ про расписание — по разбору ниши это самый заметный зазор у конкурентов.

**Не принято, с обоснованием:**
- *«55 000 фотографий непроверяемо»* — цифра настоящая, сообщество показывает «55K» в разделе фото. Оставлено.
- *«Поставить „от N ₽“ по рынку»* — заказчик решил цифр не ставить. Выдумывать суммы за реальный бизнес нельзя, поэтому секция цен снята, а не заполнена ориентирами.

## Открытые вопросы к владельцу проекта
1. **Цены.** Оба проверяющих независимо назвали отсутствие сумм главной слабостью для мамы 28-45. Если студия даст прайс — секцию цен верну на место.
2. **Расписание групп.** Публичного нет ни у кого в нише; если студия даст, это будет заметное преимущество.
3. **Свежие отзывы.** Опубликованные взяты из открытого топика, все 2020-2022 годов. В топике 98 сообщений — если есть свежие 2024-2026, стоит поставить их первыми.
4. **Логотип.** Хромированная надпись с обложки вырезалась из JPEG и вблизи мягкая, поэтому в hero стоит набранное слово. Появится исходник с прозрачностью — поставлю его.
5. **Приём заявок.** Форма собирает текст и открывает диалог с сообществом ВК (телеграм у студии — канал, в него писать нельзя). Для автоматического приёма нужен serverless-обработчик.

## Скриншоты
- shots/desktop-1440.png — первый экран
- shots/desktop-full.png — вся страница (1440×11879)
- shots/mobile-375.png — мобильная версия

## Порог показа
lint ≥ 9 ✓ (10/10) · copy без красных ✓ (10/10) · design-ru ≥ 9 ✓ (10/10) · характер ≥ 4/6 ✓ (6/6) · art-director «ПРОПУСКАЮ» — ожидается.
